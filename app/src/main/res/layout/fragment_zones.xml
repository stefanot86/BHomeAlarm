<?xml version="1.0" encoding="utf-8"?>
<!--
    Layout per la schermata di creazione scenario personalizzato.

    Struttura:
    - Toolbar con titolo e pulsante indietro
    - Area scrollabile con:
        - Campo di input per il nome dello scenario
        - Istruzioni per l'utente
        - Lista delle zone selezionabili
    - Barra inferiore fissa con conteggio zone e pulsanti Annulla/Salva

    Utilizza ConstraintLayout come root per gestire correttamente
    la tastiera virtuale (windowSoftInputMode="adjustResize" nel manifest).
-->
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?android:colorBackground"
    tools:context=".view.fragment.ZonesFragment">

    <!-- Toolbar con titolo "Crea Scenario" e freccia indietro -->
    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        app:title="@string/title_zones"
        app:navigationIcon="@drawable/ic_arrow_back"
        app:layout_constraintTop_toTopOf="parent" />

    <!--
        Area scrollabile principale.
        Contiene il campo nome, le istruzioni e la lista zone.
        fillViewport="true" assicura che il contenuto occupi tutto lo spazio disponibile.
    -->
    <androidx.core.widget.NestedScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:fillViewport="true"
        app:layout_constraintTop_toBottomOf="@id/toolbar"
        app:layout_constraintBottom_toTopOf="@id/bottom_bar">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingBottom="16dp">

            <!--
                Campo di input per il nome dello scenario.
                - Stile Material Design 3 OutlinedBox
                - Limite di 20 caratteri
                - Mostra errore se si tenta di salvare senza nome
            -->
            <com.google.android.material.textfield.TextInputLayout
                android:id="@+id/input_layout_name"
                style="@style/Widget.Material3.TextInputLayout.OutlinedBox"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginHorizontal="16dp"
                android:layout_marginTop="16dp"
                android:hint="@string/hint_scenario_name">

                <com.google.android.material.textfield.TextInputEditText
                    android:id="@+id/input_scenario_name"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:inputType="text"
                    android:maxLength="20" />

            </com.google.android.material.textfield.TextInputLayout>

            <!-- Testo istruzioni per l'utente -->
            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:paddingHorizontal="16dp"
                android:paddingTop="12dp"
                android:paddingBottom="8dp"
                android:text="@string/zones_instruction"
                android:textAppearance="?attr/textAppearanceBodyMedium" />

            <!--
                Lista delle zone selezionabili.
                Ogni item mostra checkbox + nome zona.
                nestedScrollingEnabled="false" per far funzionare correttamente
                lo scroll all'interno di NestedScrollView.
            -->
            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/recycler_zones"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:clipToPadding="false"
                android:paddingHorizontal="16dp"
                android:nestedScrollingEnabled="false"
                app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
                tools:itemCount="8"
                tools:listitem="@layout/item_zone" />

        </LinearLayout>

    </androidx.core.widget.NestedScrollView>

    <!--
        Barra inferiore fissa.
        Contiene:
        - Conteggio zone selezionate (es. "3 zone selezionate")
        - Pulsante Annulla (text button)
        - Pulsante Salva (filled button, disabilitato finché non valido)

        L'elevation crea l'effetto ombra per separare visivamente dalla lista.
    -->
    <LinearLayout
        android:id="@+id/bottom_bar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="?attr/colorSurface"
        android:elevation="8dp"
        android:gravity="end"
        android:orientation="horizontal"
        android:padding="16dp"
        app:layout_constraintBottom_toBottomOf="parent">

        <!-- Mostra il conteggio delle zone selezionate -->
        <TextView
            android:id="@+id/text_selected_count"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical"
            android:layout_weight="1"
            android:textAppearance="?attr/textAppearanceBodyMedium"
            tools:text="3 zone selezionate" />

        <!-- Pulsante per annullare e tornare indietro -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/button_cancel"
            style="@style/Widget.Material3.Button.TextButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/action_cancel" />

        <!--
            Pulsante per salvare lo scenario.
            Inizialmente disabilitato, si abilita quando:
            - È stato inserito un nome
            - È stata selezionata almeno una zona
        -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/button_save"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:enabled="false"
            android:text="@string/action_save" />

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
